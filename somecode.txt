                                        if (allroles.length >= 1) await msg.react("1⃣")
                                        if (allroles.length >= 2) await msg.react('2⃣')
                                        if (allroles.length >= 3) await msg.react('3⃣')
                                        if (allroles.length >= 4) await msg.react('4⃣')
                                        if (allroles.length >= 5) await msg.react('5⃣')
                                        if (allroles.length >= 6) await msg.react('6⃣')
                                        if (allroles.length >= 7) await msg.react('7⃣')
                                        if (allroles.length >= 8) await msg.react('8⃣')
                                        if (allroles.length >= 9) await msg.react('9⃣')